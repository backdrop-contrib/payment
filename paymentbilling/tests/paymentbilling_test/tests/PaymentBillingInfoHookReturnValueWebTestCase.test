<?php

class PaymentBillingInfoHookReturnValueWebTestCase extends PaymentWebTestCase {

  static function getInfo() {
    return array(
      'name' => "Billing's implementations of info hooks",
      'group' => 'Billing',
      'dependencies' => array('paymentbilling', 'xtools'),
    );
  }

  function setUp() {
    parent::setUp(array('paymentbilling', 'xtools'));
  }

  function testCoreHookReturnValue() {
    $this->XtoolsAssertInfoHookReturnValue('paymentbilling', array('drupal'));
  }
}