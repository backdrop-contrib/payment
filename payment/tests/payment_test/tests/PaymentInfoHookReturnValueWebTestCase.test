<?php

class PaymentInfoHookReturnValueWebTestCase extends XtoolsWebTestCase {

  static function getInfo() {
    return array(
      'name' => "Payment's implementations of info hooks",
      'group' => 'Payment',
      'dependencies' => array('payment', 'xtools'),
    );
  }

  function setUp() {
    parent::setUp(array('payment', 'xtools'));
  }

  function testCoreHookReturnValue() {
    $this->XtoolsAssertInfoHookReturnValue(array('payment', 'payment_test'), array('drupal', 'payment', 'xtools'));
  }
}