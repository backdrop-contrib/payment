<?php

class PaymentBillingCallablesWebTestCase extends PaymentWebTestCase {

  static function getInfo() {
    return array(
      'name' => "Callables",
      'group' => 'Billing',
      'dependencies' => array('paymentbilling', 'xtools'),
    );
  }

  function setUp() {
    parent::setUp(array('xtools'));
  }

  function testCallables() {
    $this->XtoolsAssertCallables('paymentbilling');
  }
}