<?php

class PaymentMethodBasicInfoHookReturnValueWebTestCase extends PaymentWebTestCase {

  static function getInfo() {
    return array(
      'name' => "Basic Payment Method's implementations of info hooks",
      'group' => 'Basic Payment Method',
      'dependencies' => array('payment', 'paymentmethodbasic', 'xtools'),
    );
  }

  function setUp() {
    parent::setUp(array('payment', 'paymentmethodbasic', 'xtools'));
  }

  function testCoreHookReturnValue() {
    $this->XtoolsAssertInfoHookReturnValue('paymentmethodbasic', array('payment'));
  }
}