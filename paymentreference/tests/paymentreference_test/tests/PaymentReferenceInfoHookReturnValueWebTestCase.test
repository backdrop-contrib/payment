<?php

class PaymentReferenceInfoHookReturnValueWebTestCase extends PaymentWebTestCase {

  static function getInfo() {
    return array(
      'name' => "Payment Reference's implementations of info hooks",
      'group' => 'Payment Reference',
      'dependencies' => array('paymentreference', 'xtools'),
    );
  }

  function setUp() {
    parent::setUp(array('paymentreference', 'xtools'));
  }

  function testCoreHookReturnValue() {
    $this->XtoolsAssertInfoHookReturnValue(array('paymentreference', 'paymentreference_test'), array('drupal'));
  }
}